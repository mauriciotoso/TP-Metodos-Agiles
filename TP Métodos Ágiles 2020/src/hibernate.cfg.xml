<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- JDBC Database connection settings -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        
        <!--<property name="connection.url">jdbc:mysql://localhost:3306/prueba?useSSL=false&amp;allowPublicKeyRetrieval=true;useJDBCCompliantTimezoneShift=true;useLegacyDatetimeCode=false;serverTimezone=UTC</property>-->
        <property name="connection.url">jdbc:mysql://localhost:3306/prueba</property>
       	<property name="connection.username">Mati</property>
        
        <property name="connection.password">SQLPass</property>

        <!-- JDBC connection pool settings ... using built-in test pool -->
        <property name="connection.pool_size">1</property>

        <!-- Select our SQL dialect -->
        <property name="dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo the SQL to stdout -->
        <property name="show_sql">true</property>
        <property name="hibernate.id.new_generator_mappings"> value="true"</property>
        <!-- Drop and re-create the database schema on startup -->
		<property name="hbm2dll.auto">create</property>
        

		<!-- Set the current session context -->
		<property name="current_session_context_class">thread</property>
		 <!-- Name the annotated entity class -->
		<mapping class="Entidades.Licencia"/>
		<mapping class="Entidades.Titular"/>

    
    </session-factory>

</hibernate-configuration>